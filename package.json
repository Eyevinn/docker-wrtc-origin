{
  "name": "docker-wrtc-origin",
  "version": "0.1.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postversion": "git push && git push --tags"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@eyevinn/whip-endpoint": "^1.0.0"
  }
}
